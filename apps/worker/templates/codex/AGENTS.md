Please provide all answers in Japanese.

When working on Excalidraw files, read `.agents/skills/excalidraw-skill/SKILL.md`.

If MCP tools are not available, use the Excalidraw skill's REST API mode or the scripts bundled with the skill.

Create `.excalidraw` files inside this workspace.

## Git workflow

- When the user asks you to implement code changes, create commits explicitly.
- Commit after each coherent unit of work, not only at the end.
- Before each commit, run the relevant tests or explain why they were not run.
- Use clear commit messages in Conventional Commits style.
- Do not amend or rewrite existing commits unless explicitly asked.
